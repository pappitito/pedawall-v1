import { CourseProp } from "@/components/misc";
import { createSlice } from "@reduxjs/toolkit";

interface UserProps {
    
}

const initialState: UserProps ={
    
}

export const userSlice = createSlice({
    name: 'user',
    initialState,
    reducers: {
        
    }
})